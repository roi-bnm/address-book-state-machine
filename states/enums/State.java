package states.enums;

/**
 * This ENUM will hold all the states available in the system
 */
public enum State {
    KNOWN,
    UNKNOWN,
    MANUAL_APPROVED,
    PENDING_VERIFICATION,
    STRONG_APPROVED
}
